{
  "name": "@ismart/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "start": "next start -p ${PORT:-3000}",
    "start:dev": "next dev -p ${PORT:-3000}",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
